import sys

inp_fn = sys.argv[1]
out_fn = 'bib.html'

inp = open(inp_fn, 'r')
out = open(inp_fn, 'w')

write = False
for l in inp:
    if 'body' in l:
        if not write:
            write = True
        else:
            write = False

    if write: out.write(l)

inp.close()
out.close()
